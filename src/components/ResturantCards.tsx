import React from 'react'

export default function ResturantCards({resturantName, vicinity, user_ratings_total, rating, photoRefrence,icon, icon_background_color, open_now, business_status, geometry}) {
  return (
    <div>
      
    </div>
  )
}
